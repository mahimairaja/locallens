name: Test Suite

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      qdrant:
        image: qdrant/qdrant:v1.14.0
        ports:
          - 6333:6333
          - 6334:6334

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry + build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('rust/Cargo.toml', 'rust/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install Python tooling
        run: |
          python -m venv .venv
          .venv/bin/pip install --upgrade pip maturin coverage
          .venv/bin/pip install -e ".[test]"
          .venv/bin/pip install -r backend/requirements.txt

      - name: Build Rust extension (locallens-core from rust/ workspace)
        run: cd rust && ../.venv/bin/maturin develop --release

      - name: Wait for Qdrant to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:6333/healthz && break
            echo "Waiting for Qdrant... ($i)"
            sleep 2
          done

      - name: Run tests (skip slow / Ollama-dependent)
        env:
          QDRANT_TEST_URL: http://localhost:6333
        run: |
          .venv/bin/coverage run -m pytest tests/ -v --tb=short -m "not slow"
          .venv/bin/coverage xml
          .venv/bin/coverage report

      - name: Upload coverage to Codecov
        if: always()
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: mahimairaja/locallens
          fail_ci_if_error: false
